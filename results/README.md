# Results

No final modelling or valuation results have been produced. The existing validation
notes in `docs/VALIDATION.md` test data-conversion correctness, not predictive quality.

Copy `EXPERIMENT_TEMPLATE.md` for every completed experiment. Commit only reviewed
aggregate metrics, methodology and charts allowed by source licenses. Keep fitted
models, row-level predictions and data extracts in approved private storage; record
their locations and hashes rather than committing them.

A result is ready for review when another teammate can reproduce it from a Git
commit, input manifest, configuration and documented environment. Negative results
and limitations are part of the report.
